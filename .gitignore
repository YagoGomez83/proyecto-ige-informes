bin/
obj/
*.user

.vs/
.vscode/

appsettings.Development.json
appsettings.*.local.json
docker/.env
docker/docker-compose.override.local.yml

# Relevamientos con datos de infraestructura sensible (ubicaciones de
# cámaras, jurisdicciones policiales) — se leen desde una ruta local, no
# se versionan (ver docs/03-modelo-dominio.md, sección de migración).
docs/*.xlsx

*.log

.claude/settings.local.json
.claude/scheduled_tasks.lock
